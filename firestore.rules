
rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read and write access to all collections.
    // This is suitable for development and prototyping but should be
    // secured with authentication rules for production.
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
